#include <vector>
#include <map>

using namespace std;

vector<map<int, float>> * nonsense(vector<int> &v, map<bool, bool>* mp){
    vector<map<int, float>> *ret = new vector<map<int, float>>;
    return ret;
}

vector<int> & address(vector<int> &v){
    return v;
}

map<int, vector<bool>> **even(int n){
    if(n % 2 == 1)
        return NULL;
    map<int, vector<bool>> ** ret = new map<int, vector<bool>> *;
    return ret;
}

int **********dereference(int  *********** ref){
    return (*ref);
}

const int **********constDereference(const int  *********** ref){
    return (*ref);
}


int main(){
    return 0;
}
